import Setup from '@TopStories/Setup';
import React from 'react';
import {SafeAreaView} from 'react-native';

const App: React.FC = () => {
    return (
        <SafeAreaView>
            <Setup />
        </SafeAreaView>
    );
};

export default App;
